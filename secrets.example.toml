# .streamlit/secrets.toml
# Rename this file to 'secrets.toml' inside .streamlit folder to enable real emails

[email]
smtp_server = "smtp.gmail.com"
smtp_port = 587
sender_email = "your-email@gmail.com"
app_password = "your-app-password" # Generate at myaccount.google.com/apppasswords
